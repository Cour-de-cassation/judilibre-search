apiVersion: v1
kind: Service
metadata:
  name: {{ include "judilibre-search.fullname" . }}
  labels:
    {{- include "judilibre-search.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "judilibre-search.selectorLabels" . | nindent 4 }}

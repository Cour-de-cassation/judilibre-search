apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ${APP_ID}-ingress
  namespace: ${APP_GROUP}
spec:
  tls:
  - hosts:
    - ${APP_HOST}
    secretName: ${APP_ID}-tls
  rules:
  - host: ${APP_HOST}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ${APP_ID}-svc
            port:
              number: 80

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ${API_ID}-ingress
spec:
  tls:
  - hosts:
    - search.judilibre.io
    secretName: ${API_ID}-tls
  rules:
  - host: search.judilibre.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ${API_ID}-svc
            port:
              number: 80

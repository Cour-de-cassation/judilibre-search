apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ${APP_ID}-ingress
  namespace: ${APP_GROUP}
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ${APP_ID}-svc
            port:
              number: 80

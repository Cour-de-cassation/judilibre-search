apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: judilibre-search-ingress
spec:
  rules:
  - host: search.judilibre.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: judilibre-svc
            port:
              number: 80
